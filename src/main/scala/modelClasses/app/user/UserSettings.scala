package modelClasses.app.user

import modelClasses.ids.User.UserId

case class UserSettings(
                       // user    : UserId,
                       isPrivate: Boolean
                       )


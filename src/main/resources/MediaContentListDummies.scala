import modelClasses.app.social.MediaContentList

import modelClasses.ids.Media.{MovieId, TVShowId, VideogameId, BookId}
import modelClasses.ids.Social.{LikeId, MediaContentListId, ReplyId}
import modelClasses.ids.User.UserId

object MediaContentListDummies {
  val mediaContentList1: MediaContentList = new MediaContentList()
  val mediaContentList2: MediaContentList = new MediaContentList()
  val mediaContentList3: MediaContentList = new MediaContentList()
  val mediaContentList4: MediaContentList = new MediaContentList()
  val mediaContentList5: MediaContentList = new MediaContentList()
}
import modelClasses.app.social.Entry
import modelClasses.app.Time

import modelClasses.ids.Media._
import modelClasses.ids.Social.{EntryId, RatingId, ReviewId}
import modelClasses.ids.User.UserId

object EntryDummies {
  val entry1: Entry = new Entry()
  val entry2: Entry = new Entry()
  val entry3: Entry = new Entry()
  val entry4: Entry = new Entry()
  val entry5: Entry = new Entry()
}